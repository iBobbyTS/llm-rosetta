# Install codex-rosetta with gateway extras (starlette, uvicorn, httpx)
# From the repo root:  pip install -e ".[gateway]"
# Or standalone:       pip install "codex-rosetta[gateway]"
codex-rosetta[gateway]

# Usage:
#   codex-rosetta-gateway --config /path/to/config-directory
#   python -m codex_rosetta.gateway --config /path/to/config-directory
# Optional Codex Home: set CODEX_HOME or pass --codex-home /path/to/codex-home
