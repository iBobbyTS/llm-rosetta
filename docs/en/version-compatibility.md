# Codex CLI Version Compatibility

Codex-Rosetta release numbers follow the Codex CLI version they support. A
Codex-Rosetta version therefore identifies the compatible Codex CLI version,
rather than an independent project release sequence.

## Current Compatibility

Codex-Rosetta `0.142.5` is compatible with Codex CLI `0.142.5`.
