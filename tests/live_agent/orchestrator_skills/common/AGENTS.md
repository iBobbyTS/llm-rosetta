# Orchestrator Skills Test Workspace

- Follow `TASK.md` exactly and preserve the listed call order.
- Use only the native `skills.list` and `skills.read` Namespace tools.
- Do not use local Skill discovery, files, shell, browser, network, MCP, plugin,
  or app tools as substitutes.
- Do not modify any resource.
- Keep the final response to the exact result line requested by the task.
