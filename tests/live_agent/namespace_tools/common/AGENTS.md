# Clock And Memory Namespace Test Workspace

This workspace tests only direct Namespace tool use.

- Follow `TASK.md` exactly and perform the operations in its stated order.
- Do not use shell commands, browser tools, local file tools, network search,
  filesystem-backed Skills, or orchestrator Skills as substitutes.
- Do not modify files or memories.
- Keep the final response to the exact result line requested by the task.
