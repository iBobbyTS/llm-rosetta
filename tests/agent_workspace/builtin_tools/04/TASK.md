Call `view_image` exactly once with path `fixtures/quadrants.png` and detail
`original`. Do not read the file, convert it, or use another image or file tool.

If `view_image` returns a non-error image result, reply with only
`RESULT:VIEW_IMAGE_OK`. Otherwise reply with only `RESULT:VIEW_IMAGE_FAILED`.
