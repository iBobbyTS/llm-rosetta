# Image Generation Test Workspace

This workspace is a deterministic image-generation and visual-inspection test.

- Follow `TASK.md` exactly and preserve its call order.
- Use only the model-facing tools named by the task.
- Do not inspect parent directories or unrelated files.
- Do not substitute shell commands, browser automation, direct file reads,
  Skills, MCP, image conversion, or prior knowledge for either required tool.
- Do not modify or delete the generated artifact.
- Keep the final response to the exact result-line format requested by the task.
