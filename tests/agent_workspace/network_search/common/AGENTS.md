# Network Search Test Workspace

Follow `TASK.md` exactly. This workspace tests only use of the available
network-search surface and the named `web.run` operations. Do not use shell
commands, direct HTTP clients, external browser tools, local file inspection,
or prior knowledge as substitutes. Browser and PDF work performed behind a
valid `web.run` call is part of the target operation, not a fallback.
