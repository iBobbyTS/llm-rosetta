Use the available network search capability to search the web for the official
Python 3 documentation. Do not use shell commands, browser automation, local
file inspection, or prior knowledge as a substitute for network search. If the
search tool returns at least one result whose URL is on `docs.python.org`, reply
with only `RESULT:NETWORK_SEARCH_OK`. If the search fails or returns no
`docs.python.org` URL, reply with only `RESULT:NETWORK_SEARCH_FAILED`.
