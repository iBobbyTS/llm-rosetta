Start `python3 scenario.py` once in a mode that allows later interaction,
arranging for the first command result to return within 1000 milliseconds.
After the program prints `INPUT:VALUE`, continue that same process session and
send exactly `rosetta` followed by a newline. Do not restart the process and do
not run another command. Reply with only the complete line that starts with
`RESULT:`.
