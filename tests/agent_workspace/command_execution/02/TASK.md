Start `python3 scenario.py` once, arranging for the first command result to
return within 1000 milliseconds. The process will still be running. Do not
restart it and do not run another command. Continue the returned process
session by polling it without sending input until it exits. Reply with only the
complete line that starts with `RESULT:`.
